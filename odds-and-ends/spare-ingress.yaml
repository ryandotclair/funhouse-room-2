# DO NOT APPLY: scrap from Goon3's failed route experiment
# NKP already fronts Traefik. Copying this blindly is how you get a second opinion
# you did not ask for.
#
# Rough shape the live desk page used last time anyone checked:
#   Host(`riddler.rycr.io`) && PathPrefix(`/r/<full-token>/`)
#
# middlewares lie, paths don't
# TOKEN_PART_B=ld5Alt
#
# Goon17: if CD just "undid" your manifest, check who was near the red console.
#
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: scrap-do-not-apply
  annotations:
    # Goon91: why is "panic rollback" a first-class feature
    goon.notes/status: "orphaned-on-purpose"
spec:
  routes: []
