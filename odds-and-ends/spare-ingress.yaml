# DO NOT APPLY — scrap from last booth teardown
# (NKP + Traefik leftovers. The live route lives in the cabinet.)

# match: Host(`riddler.rycr.io`) && PathPrefix(`/r/…`)
#
# middlewares lie, paths don't
# TOKEN_PART_B=6vj2Av
#
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: scrap-do-not-apply
spec:
  routes: []
