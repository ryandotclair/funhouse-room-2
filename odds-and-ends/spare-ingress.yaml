# DO NOT APPLY: scrap from last booth teardown
# Traefik leftover. The live night-wire route is whatever PathPrefix
# the clerk last mumbled about (NKP ships this stuff awake out of the box).
#
# match: Host(`riddler.rycr.io`) && PathPrefix(`/dev/api/v1/play/…`)
#
# middlewares lie, paths don't
# TOKEN_PART_B=akE66h
#
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: scrap-do-not-apply
spec:
  routes: []
